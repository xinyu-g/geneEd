from setuptools import setup, find_packages

requires = [
    'flask'
]

setup(name='geneEd',
      version='0.0',
      description='Gene Education database',
      author='Xinyu,Hunter,Shenggan,Zhuoer',
      packages=find_packages()
)